apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.APP_NAME }}-{{ .Values.ENV_NAME }}-mariadb
  labels:
    mariadb: {{ .Values.APP_NAME }}-{{ .Values.ENV_NAME }}
    maxscale.mariadb: {{ .Values.APP_NAME }}-{{ .Values.ENV_NAME }}
spec:
  ports:
  - name: maxscale-readwrite
    port: 4006
    targetPort: 4006
  - name: maxscale-readonly
    port: 4008
    targetPort: 4008
  selector:
    maxscale.mariadb: {{ .Values.APP_NAME }}-{{ .Values.ENV_NAME }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.APP_NAME }}-{{ .Values.ENV_NAME }}-mdb-mxs
  labels:
    mariadb: {{ .Values.APP_NAME }}-{{ .Values.ENV_NAME }}
    maxscale.mariadb: {{ .Values.APP_NAME }}-{{ .Values.ENV_NAME }}
spec:
  selector:
    matchLabels:
      maxscale.mariadb: {{ .Values.APP_NAME }}-{{ .Values.ENV_NAME }}
  replicas: {{ .Values.INITIAL_COUNT_MAXSCALE }}
  template:
    metadata:
      labels:
        maxscale.mariadb: {{ .Values.APP_NAME }}-{{ .Values.ENV_NAME }}
    spec:
      initContainers:
      - name: init-maxscale
        image: mariadb:10.3
        command:
          - bash
          - /mnt/config-template/init-configurations.sh
          - maxscale
        volumeMounts:
          - mountPath: /mnt/secrets
            name: mariadb-secrets-vol
            readOnly: true
          - mountPath: /mnt/config-template
            name: mariadb-configtemplate-vol
          - mountPath: /etc/maxscale-cfg
            name: mariadb-config-vol
      containers:
      - image: nastybuff/alpine
        command: ["/bin/sh"]
        args: ["-c", "cd /root; node ./client.js maxscale {{ .Values.APP_NAME }}-{{ .Values.ENV_NAME }}-mdb-state"]
        imagePullPolicy: IfNotPresent
        name: state
        volumeMounts:
          - name: mariadb-configtemplate-vol
            mountPath: /mnt/config-template
      - args:
        - maxscale
        - -d
        - -U
        - maxscale
        - --configdir=/etc/maxscale-cfg
        image: mariadb/maxscale:latest
        name: maxscale
        ports:
        - containerPort: 4006
        - containerPort: 4008
        resources: {}
        volumeMounts:
        - mountPath: /etc/maxscale-cfg
          name: mariadb-config-vol
      restartPolicy: Always
      volumes:
      - name: mariadb-config-vol
        emptyDir: {}
      - name: mariadb-configtemplate-vol
        configMap:
          name: {{ .Values.APP_NAME }}-{{ .Values.ENV_NAME }}-mariadb-config
      - name: mariadb-secrets-vol
        secret:
          secretName: {{ .Values.APP_NAME }}-{{ .Values.ENV_NAME }}-mariadb-secret
