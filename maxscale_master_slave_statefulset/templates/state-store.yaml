kind: Service
apiVersion: v1
metadata:
  name: {{MARIADB-LABEL}}-mdb-state
  labels:
    mariadb: {{MARIADB-LABEL}}
    state-store.mariadb: {{MARIADB-LABEL}}
spec:
  selector:
    state-store.mariadb: {{MARIADB-LABEL}}
  ports:
  - protocol: TCP
    port: 80
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{MARIADB-LABEL}}-mdb-state
  labels:
    mariadb: {{MARIADB-LABEL}}
    state-store.mariadb: {{MARIADB-LABEL}}
spec:
  replicas: 1
  selector:
    matchLabels:
      state-store.mariadb: {{MARIADB-LABEL}}
  template:
    metadata:
      labels:
        state-store.mariadb: {{MARIADB-LABEL}}
    spec:
      containers:
      - image: nastybuff/alpine
        command: ["/bin/sh"]
        args: ["-c", "cd /root; node ./server.js"]
        imagePullPolicy: IfNotPresent
        name: state-store
        ports:
        - containerPort: 80
        volumeMounts:
          - name: mariadb-configtemplate-vol
            mountPath: /mnt/config-template
      volumes:
      - name: mariadb-configtemplate-vol
        configMap:
          name: mariadb-config
      restartPolicy: Always
