# Headless service for stable DNS entries for StatefulSet members
apiVersion: v1
kind: Service
metadata:
  name: {{MARIADB-LABEL}}-mdb-clust
  labels:
    mariadb: {{MARIADB-LABEL}}
spec:
  ports:
  - name: mariadb
    port: 3306
    targetPort: 3306
  clusterIP: None
  selector:
    mariadb: {{MARIADB-LABEL}}
---   
# a StatefulSet for the master/slave cluster 
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{MARIADB_LABEL}}-mdb-ms
  labels:
    mariadb: {{MARIADB_LABEL}}
    server.mariadb: {{MARIADB_LABEL}}
spec:
  selector:
    matchLabels:
      server.mariadb: {{MARIADB_LABEL}}
  serviceName: {{MARIADB_LABEL}}-mdb-clust
  replicas: 3
  template:
    metadata:
      labels:
        mariadb: {{MARIADB_LABEL}}
        server.mariadb: {{MARIADB_LABEL}}
    spec:
      initContainers:
      # runs a state store client to determine the IP of the active master
      - name: init-get-master
        image: node:alpine
        command: ["/bin/sh"]
        args: ["-c", "cd /root; tar xzf /mnt/config-map/state-store.tar.gz; node ./client.js mariadb-init {{MARIADB_LABEL}}-mdb-state /mnt/config-map"]
        imagePullPolicy: IfNotPresent
        volumeMounts:
          - mountPath: /mnt/config-map
            name: mariadb-config-vol
      # runs an initialization script to prepare the correct startup configuration
      - name: init-masterslave
        image: mariadb:10.3
        command:
          - bash
          - /mnt/config-template/init-configurations.sh
          - masterslave
        volumeMounts:
          - mountPath: /mnt/secrets
            name: mariadb-secrets-vol
            readOnly: true
          - name: mariadb-entrypoint-vol
            mountPath: /docker-entrypoint-initdb.d
          - mountPath: /mnt/config-template
            name: mariadb-configtemplate-vol
          - mountPath: /mnt/config-map
            name: mariadb-config-vol
      containers:
      - command:
        - bash
        # start a mariadb instance
        - /mnt/config-map/mariadb-server/start-mariadb-instance.sh 
        env:
        - name: MYSQL_ALLOW_EMPTY_PASSWORD
          value: "Y"
        image: mariadb:10.3
        name: mariadb-masterslave
        ports:
        - containerPort: 3306
        resources: {}
        volumeMounts:
        - name: mariadb-entrypoint-vol
          mountPath: /docker-entrypoint-initdb.d
        - name: mariadb-config-vol
          mountPath: /mnt/config-map
        # a permanent volume for data storage
        - name: data
          mountPath: /var/lib/mysql
          subPath: mysql
        livenessProbe:
          exec:
            command: ["mysqladmin", "ping"]
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
        readinessProbe:
          exec:
            # Check we can execute queries over TCP (skip-networking is off).
            command: ["mysql", "-h", "127.0.0.1", "-e", "SELECT 1"]
          initialDelaySeconds: 5
          periodSeconds: 2
          timeoutSeconds: 1
      # runs a state store client to ensure the state is being maitained
      - name: init-getmaster
        image: node:alpine
        command: ["/bin/sh"]
        args: ["-c", "cd /root; tar xzf /mnt/config-map/state-store.tar.gz; node ./client.js mariadb {{MARIADB_LABEL}}-mdb-state /mnt/config-map"]
        imagePullPolicy: IfNotPresent
        volumeMounts:
          - mountPath: /mnt/config-map
            name: mariadb-config-vol
      restartPolicy: Always
      volumes:
      - name: mariadb-entrypoint-vol 
        emptyDir: {}
      - name: mariadb-config-vol
        emptyDir: {}
      - name: mariadb-configtemplate-vol
        configMap:
          name: mariadb-config
      - name: mariadb-secrets-vol
        secret:
          secretName: {{APPLICATION}}-{{ENVIRONMENT}}-mariadb-secret
  # ensures persistent volumes end up on the same pod
  volumeClaimTemplates:
  - metadata:
      name: data
      labels:
        mariadb: {{MARIADB-LABEL}}
        server.mariadb: {{MARIADB-LABEL}}
    spec:
      accessModes: ["ReadWriteOnce"]
      resources:
        requests:
          storage: {{MARIADB-VOLUME-SIZE}}Gi
